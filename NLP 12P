import nltk
from nltk import CFG
from nltk.parse import EarleyChartParser

grammar = CFG.fromstring("""
S -> NP VP
NP -> 'the' N
VP -> V NP
N -> 'cat' | 'dog'
V -> 'chased'
""")

parser = EarleyChartParser(grammar)
sentence = "the cat chased dog".split()

for tree in parser.parse(sentence):
    print(tree)
